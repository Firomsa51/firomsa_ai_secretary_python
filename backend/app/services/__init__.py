"""
Services package — business logic layer between API routes and the database.

Phase 2 will add:
- UserService      — upsert Telegram users on first contact
- ConversationService — open/close/categorise conversations
- MessageService   — persist incoming and outgoing messages
- SettingsService  — read/write the singleton settings row
"""
